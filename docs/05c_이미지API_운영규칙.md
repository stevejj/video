# 05c. 이미지 API(Nano Banana) 운영 규칙 (v1.0)

> 2026-09-16 사용자 결정: 이미지는 Gemini 앱 수작업 대신 **Google Nano Banana API**로 내가 생성한다. 충전 10,000원.
> 원칙: **한 장씩 생성 → 보여주기 → 사용자 판단**. 배치는 사용자가 범위를 지정한 경우에만, 리스크·예상 비용을 적고 "진행할까요?"를 물어 "알겠다"를 받은 뒤 실행.

## 1. 모델·단가 (ai.google.dev 가격표, 2026-09-16 확인, 환율 1,400원/$ 가정)

| 모델 | 1K | 2K | 4K | 참조 이미지 | 비고 |
|---|---|---|---|---|---|
| gemini-3.1-flash-image (Nano Banana 2) | $0.067 ≈ 94원 | $0.101 ≈ 141원 | $0.151 ≈ 211원 | 물체 10 / 캐릭터 4 / 스타일 3 | **기본** |
| gemini-3-pro-image (Nano Banana Pro) | $0.134 ≈ 188원 | 188원 | $0.24 ≈ 336원 | 물체 6 / 캐릭터 5 | Flash 2회 실패 시 대안 |
| gemini-3.1-flash-lite-image | $0.034 ≈ 47원 | 1K만 | — | 물체 14 | 초안·구도 확인용 |

입력(참조 이미지·프롬프트) 비용은 장당 1~3원 수준으로 무시 가능. API 출력에는 눈에 보이는 별 워터마크가 없다(SynthID 비가시 워터마크만).

## 2. 해상도 정책
- 부위 시트·표정 시트 등 **참조용**: 1K (94원)
- 공간 기준 컷·컷별 시작/끝 프레임(**Kling 입력**): 2K 9:16 = 약 1152x2048 (141원). 1K는 576x1024라 720p 영상 입력으로도 부족.

## 3. 예상 비용 (ep01)
| 묶음 | 장수 | 단가 | 소계 |
|---|---|---|---|
| 1차 부위 시트 4 + 스케일 1 | 5 | 94~141원 | 약 600원 |
| 2차 공간 기준 컷 | 10 | 141원 | 약 1,400원 |
| 3차 프레임(신규 24컷, 시작+끝 약 40장) | 40 | 141원 | 약 5,600원 |
| 재시도 여유 30% | | | 약 2,300원 |
| **합계** | | | **약 9,900원** |
10,000원에 거의 꽉 찬다. 재시도가 많으면 초과하므로 누적 비용을 매 호출 후 보고하고, 8,000원 도달 시 경고한다.

## 4. 절차
1. 사용자가 "다음 이미지 진행"이라고 하면 → 프롬프트·참조·해상도·예상 비용을 한 줄로 보이고 실행(한 장).
2. 결과 이미지를 보여주고 06 체크리스트로 검수 결과 첨부 → 사용자 판단(채택 / 수정 재생성 / 폐기).
3. 배치 요청("P-1~P-4 한 번에")이 오면 → **범위, 각 장 프롬프트 요약, 리스크(앞 장 결과를 못 보고 만드는 데서 오는 것), 예상 비용, 진행 여부 질문** → "알겠다" 후 실행.
4. 매 호출 `outputs/nb_log.jsonl`에 기록, 누적 비용 표시.
5. 재시도: 사용자 지시 없이는 재생성하지 않는다. (자동 재시도 없음)

## 5. 키 관리
- API 키는 환경변수 `GEMINI_API_KEY` 또는 저장소 루트 `.env`(gitignore 됨)에만 둔다. 채팅·문서·커밋에 넣지 않는다.
- 권장 전달 경로: Claude Code 환경 설정의 환경변수. 채팅으로 주면 .env에 옮기고 채팅 기록에 남으므로 작업 후 키 교체를 권장.

## 6. 도구
`tools/nb_generate.py` — 한 장 생성, 참조 이미지 첨부, 비율·해상도 지정, 비용 로그.
